appMainModule.controller("registerCtrl", ["$scope", function ($scope) {

    //$scope.submitForm = function (isValid) {

    //    if (isValid) {

    //        var inputVal = { Username: this.username, Password: this.pwd };

    //        loginService.isValidUser(inputVal, function (response) {
    //            if (response) {
    //                alert("approved!!!!!!!!!!!");
    //            } else {
    //                alert("deny!!!!!!!!!!!");
    //            }
    //        });
    //    }
    //};
}]);